# Twitch-Streamer-Database
A php project to add or remove twitch streamers in a file to iframe on a website

Upload files to a webserver. Configure db_config.php for mysql db. use schema to create table
